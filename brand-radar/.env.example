# Copy to `.env` and fill in your values:  cp .env.example .env

# --- What to track ---
QUERY=your brand or keyword
SOURCE=reddit          # where to search
# MAX_LIST=5           # how many new mentions to list per alert

# Add a one-line sentiment read via pay claude (1 = on).
# SUMMARIZE=1

# --- Alert transport: telegram | webhook | websocket | stdout ---
ALERT_SINK=telegram

# telegram sink (create a bot via @BotFather, get your id via @userinfobot):
TELEGRAM_BOT_TOKEN=123456:ABC-DEF_your_bot_token
TELEGRAM_CHAT_ID=123456789

# webhook sink — POSTs the JSON payload to this URL:
# WEBHOOK_URL=https://your-service.example.com/mentions

# websocket sink — pushes the JSON payload to this WS URL (needs `websocat`):
# WS_URL=ws://localhost:8080

# stdout sink needs nothing — the JSON payload is printed for you to pipe.

# --- pay.sh social endpoint (optional; override if your route differs) ---
# PAYSH_SOCIAL_URL=https://social.pay.sh/search
