# Copy to `.env` and fill in your values:  cp .env.example .env

# --- Input / output ---
INPUT_CSV=./leads.csv       # must have a header row
KEY_COLUMN=email            # the header to look up on (email or domain)
# OUTPUT_CSV=./enriched.csv  # default: print to stdout

# --- pay.sh enrichment endpoint (optional; override if your route differs) ---
# PAYSH_ENRICH_URL=https://enrich.pay.sh/lookup
