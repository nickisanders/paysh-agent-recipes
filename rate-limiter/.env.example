# Copy to `.env` and source it, or set these inline in front of a call.

# Max calls allowed per window.
RATE_MAX=30

# The window, in seconds.
RATE_WINDOW_SEC=60

# Separate buckets so different jobs are limited independently.
# RATE_KEY=default

# Where the call log lives.
# RATE_STATE_DIR=~/.rate-limiter
