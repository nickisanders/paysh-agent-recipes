# Copy to `.env` and fill in your values:  cp .env.example .env

# --- What to research ---
ADDRESS=0x0000000000000000000000000000000000000000
CHAIN=ethereum
# SYMBOL=MEME          # optional; otherwise taken from market data

# --- Delivery: stdout | telegram | webhook | websocket ---
ALERT_SINK=stdout

# telegram sink (create a bot via @BotFather, get your id via @userinfobot):
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF_your_bot_token
# TELEGRAM_CHAT_ID=123456789

# webhook sink — POSTs the JSON payload (all four sources) to this URL:
# WEBHOOK_URL=https://your-service.example.com/dossier

# websocket sink — pushes the JSON payload to this WS URL (needs `websocat`):
# WS_URL=ws://localhost:8080

# --- pay.sh endpoints (optional; override any whose route differs) ---
# PAYSH_AUDIT_URL=https://audit.pay.sh/contract
# PAYSH_MARKET_URL=https://market.pay.sh/price
# PAYSH_ONCHAIN_URL=https://onchain.pay.sh/token
# PAYSH_SOCIAL_URL=https://social.pay.sh/search
